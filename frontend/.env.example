# Copy to .env.local and adjust if the backend isn't on the default port.
# Server-side only (no NEXT_PUBLIC_ prefix) — API calls are made from Server
# Actions/Components, never directly from the browser.
API_URL=http://localhost:8000/api/v1
